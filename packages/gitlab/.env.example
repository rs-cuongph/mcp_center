# ---------------------------------------------------------------------------
# GitLab connection
# ---------------------------------------------------------------------------

# Base URL of the self-hosted GitLab instance (no trailing slash)
GITLAB_URL=https://devops.runsystem.info

# Personal/Project Access Token (REQUIRED).
# Scope: read_api is enough for the read-only tool set in this package.
GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx

# ---------------------------------------------------------------------------
# Logging
# ---------------------------------------------------------------------------

LOG_LEVEL=info
